// Accept Values User from find is Fixing Numbers.

const prompt = require('prompt-async');

let id;
let numbers;
let name = 10;
let num;

{   
    if(name == 10) {
        console.log(`Enter is greater than 10 of Numbers`, numbers);
    } else {
        console.log(`Enter is 10 number of Fixing Numbers`, number);
    } else (num == 20) {
        console.log(`Enter is Not Valid a Number`, number);
    } else-if {
        console.log(`Enter is valid a Number `, number);
    }
}

let number = parseInt(input);

console.log(`The Find is All of Fixing ${num} Numbers is.: `, numbers);

